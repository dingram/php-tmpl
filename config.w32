// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("tmpl", "for tmpl support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("tmpl", "enable tmpl support", "no");

if (PHP_TMPL != "no") {
	EXTENSION("tmpl", "tmpl.c");
}

